# Resultado 0x0 — Plano de Implementação

> **Para agentes:** SUB-SKILL OBRIGATÓRIA: use `superpowers:subagent-driven-development`
> (recomendada) ou `superpowers:executing-plans` para executar tarefa a tarefa. Os passos
> usam checkbox (`- [ ]`) para acompanhamento.

**Goal:** registrar operação encerrada no zero a zero como um resultado próprio (`zero`),
com P&L travado em 0 e fora do denominador do win rate.

**Arquitetura:** um terceiro valor no `type` da tabela `trades` — o mesmo eixo que já
carrega `take`/`stop` —, propagado do schema para o modal, os KPIs e a renderização. Não
há coluna nova, migração de dados nem mudança em `rowToTrade()`: o tipo viaja cru do
banco até a tela.

**Spec:** `docs/superpowers/specs/2026-07-27-resultado-0x0-design.md`

**Tech Stack:** HTML/CSS/JS vanilla (ES Modules), sem build; Supabase (Postgres + RLS);
verificação com `puppeteer-core` + o stub de `.claude/skills/verify/`.

## Global Constraints

- **Identificador no banco: `'zero'`. Rótulo na UI: `0x0`.** Nunca o inverso.
- **0x0 tem `pnl` exatamente 0.** Não existe 0x0 com valor.
- **Win rate = `winTrades ÷ (count − zeroTrades)`.** Sem operação decidida, exibir `—`.
- **"Operações registradas" e "média por operação" continuam contando o 0x0.**
- Todo texto de UI em pt-BR com acentuação correta.
- Vanilla JS + ES Modules, sem framework e sem build.
- Toda I/O de dados passa por `js/services/*` — nada de `supabase` direto no `app.js`.
- Cores neutras saem de variáveis existentes (`--bg-card-hover`, `--border-color`,
  `--text-secondary`), que já existem nos temas dark e light. Nenhuma cor nova.

## Estrutura de arquivos

| Arquivo | Responsabilidade nesta mudança |
|---------|-------------------------------|
| `supabase/schema.sql` | Aceitar `'zero'` no check do `type` (banco novo e banco existente) |
| `js/services/trades.js` | Whitelist de tipos no import em lote e P&L forçado a 0 no `zero` |
| `index.html` | `<option>` do 0x0 no `#trade-type` |
| `app.js` | Travar/destravar o campo de valor, salvar com P&L 0, KPIs e renderização |
| `style.css` | `.slot-zero`, `.type-zero`, `.pnl-neutro` |
| `CLAUDE.md` | Registrar o terceiro tipo e a regra do win rate |

Sem arquivo novo: cada mudança cabe no módulo que já é dono daquela responsabilidade.

## Sobre "testes" neste projeto

O Monolith **não tem runner de teste unitário** — só um teste de compressão de imagem em
HTML. O análogo local é a skill `verify`: navegador real com o Supabase stubado. O ciclo
de cada tarefa é, então: **escrever o cenário de verificação → rodar e ver falhar →
implementar → rodar e ver passar → commitar.** O arquivo de cenários vive no scratchpad,
fora do repo (a skill `verify` proíbe `node_modules` no repositório).

⚠️ **O stub não valida constraint de banco.** Ele é um objeto em `localStorage`: aceita
`type: 'zero'` mesmo sem a migração ter rodado. Passar na verificação local **não prova**
que produção aceita. A aplicação do SQL no Supabase é o passo manual da Tarefa 1.

---

### Tarefa 1: Banco aceita `zero`, e o import não deixa passar lixo

**Arquivos:**
- Criar: `<SCRATCH>/rig/verificar-0x0.mjs` (fora do repo)
- Modificar: `supabase/schema.sql:29` e fim do arquivo (nova seção 10)
- Modificar: `js/services/trades.js:134-158` (`bulkImportTrades`)

Onde se lê `<SCRATCH>`, use:
`C:/Users/nikol/AppData/Local/Temp/claude/C--Users-nikol-Desktop-Niko/f1ef0695-32ac-4b4e-a00e-18e9413d1774/scratchpad`

**Interfaces:**
- Produz: `TIPOS_VALIDOS` (Set privado de `trades.js`) e a garantia de que nenhuma linha
  gravada por import tem `type` fora de `take|stop|zero`, nem `zero` com `pnl ≠ 0`.
- Produz: o harness `verificar-0x0.mjs`, com as funções `novaPagina()`, `semear(page,
  linhas)` e `conferir(nome, ok, detalhe)`, que as Tarefas 2 a 5 reutilizam.

- [ ] **Passo 1: Preparar o rig (uma vez só)**

```bash
mkdir -p "<SCRATCH>/rig"
cd "<SCRATCH>/rig" && npm init -y >/dev/null && npm i puppeteer-core
cd "C:/Users/nikol/Desktop/Niko/projetos/apps/Monolith" && python -m http.server 8077 &
```

Confira que o servidor subiu: `curl -s -o /dev/null -w "%{http_code}" http://localhost:8077/`
deve responder `200`. Se a porta estiver ocupada, use outra e ajuste `BASE` no harness.

- [ ] **Passo 2: Escrever o harness com o primeiro cenário (falhando)**

Crie `<SCRATCH>/rig/verificar-0x0.mjs`:

```js
import fs from 'node:fs';
import puppeteer from 'puppeteer-core';

const RAIZ = 'C:/Users/nikol/Desktop/Niko/projetos/apps/Monolith';
const STUB = fs.readFileSync(`${RAIZ}/.claude/skills/verify/stub-supabase-client.js`, 'utf8');
const BASE = 'http://localhost:8077';
const CHROME = 'C:/Program Files/Google/Chrome/Application/chrome.exe';

const falhas = [];
function conferir(nome, ok, detalhe = '') {
  console.log(`  ${ok ? 'ok   ' : 'FALHA'} ${nome}${ok ? '' : '  → ' + detalhe}`);
  if (!ok) falhas.push(nome);
}
const dormir = (ms) => new Promise((r) => setTimeout(r, ms));

const navegador = await puppeteer.launch({
  executablePath: CHROME, headless: 'new', args: ['--no-sandbox']
});

async function novaPagina() {
  const page = await navegador.newPage();
  await page.setViewport({ width: 1440, height: 900 });
  await page.setRequestInterception(true);
  page.on('request', (req) => {
    if (req.url().includes('/js/supabase-client.js')) {
      req.respond({ status: 200, contentType: 'application/javascript; charset=utf-8', body: STUB });
    } else req.continue();
  });
  page.on('dialog', (d) => d.accept());
  await page.goto(BASE, { waitUntil: 'networkidle2' });
  await page.waitForSelector('#app-shell:not(.hidden)');
  await dormir(700);
  return page;
}

/** Escreve trades direto no "banco" do stub e recarrega para o app lê-los. */
async function semear(page, linhas) {
  await page.evaluate((linhas) => {
    localStorage.setItem('__stub_db__', JSON.stringify({
      trades: linhas.map((l, i) => ({
        id: 'seed-' + i, user_id: 'stub-user-1', block_index: 1, position: i,
        asset: l.asset || 'WIN', type: l.type, pnl: l.pnl,
        trade_date: '2026-07-27', notes: null, images: [], risk_reward: null,
        created_at: '2026-07-27T12:00:0' + (i % 10) + '.000Z',
        updated_at: '2026-07-27T12:00:00.000Z'
      })),
      user_preferences: [{ user_id: 'stub-user-1', active_block_index: 1, theme: 'dark' }],
      trading_plans: []
    }));
  }, linhas);
  await page.reload({ waitUntil: 'networkidle2' });
  await page.waitForSelector('#app-shell:not(.hidden)');
  await dormir(700);
}

const lerBanco = (page) => page.evaluate(() => JSON.parse(localStorage.getItem('__stub_db__')));

// ---------------------------------------------------------------- Cenário 1
console.log('\n1. Import de JSON com type "zero"');
{
  const page = await novaPagina();
  const backup = {
    activeBlockIndex: 1,
    blocks: { '1': [
      { asset: 'WIN', type: 'take', pnl: 100, date: '2026-07-27', notes: '' },
      { asset: 'WIN', type: 'zero', pnl: 300, date: '2026-07-27', notes: '' },
      { asset: 'WIN', type: 'gato', pnl: 10,  date: '2026-07-27', notes: '' }
    ] }
  };
  const arquivo = `<SCRATCH>/rig/backup-0x0.json`;
  fs.writeFileSync(arquivo, JSON.stringify(backup));
  const input = await page.$('#file-import-input');
  await input.uploadFile(arquivo);
  await dormir(1200);

  const db = await lerBanco(page);
  const zero = db.trades.find((t) => t.asset === 'WIN' && t.type === 'zero');
  conferir('grava type "zero"', !!zero, JSON.stringify(db.trades.map((t) => t.type)));
  conferir('força pnl 0 no zero', zero && Number(zero.pnl) === 0, zero && String(zero.pnl));
  conferir('tipo desconhecido vira take',
    db.trades.filter((t) => t.type === 'take').length === 2,
    JSON.stringify(db.trades.map((t) => t.type)));
  await page.close();
}

// ---------------------------------------------------------------- Resultado
await navegador.close();
console.log(falhas.length ? `\n${falhas.length} FALHA(S): ${falhas.join(', ')}` : '\nTudo passou.');
process.exit(falhas.length ? 1 : 0);
```

- [ ] **Passo 3: Rodar e ver falhar**

```bash
node "<SCRATCH>/rig/verificar-0x0.mjs"
```

Esperado: FALHA em `grava type "zero"` e `força pnl 0 no zero` — hoje
`bulkImportTrades` reescreve qualquer tipo que não seja `stop` como `take`.

- [ ] **Passo 4: Abrir o `type` no schema**

Em `supabase/schema.sql:29`, troque:

```sql
  type        text not null check (type in ('take','stop')),
```

por:

```sql
  type        text not null check (type in ('take','stop','zero')),
```

E acrescente ao **fim do arquivo** a seção 10:

```sql
-- ----------------------------------------------------------------------------
-- 10. Resultado 0x0 (zero a zero)
--     'zero' = operação encerrada no preço de entrada, com pnl sempre 0.
--     A constraint do type nasce inline no create table lá em cima (o Postgres
--     a nomeia trades_type_check), e `create table if not exists` não roda de
--     novo em quem já tem a tabela — por isso o valor novo só chega aos bancos
--     existentes por este alter. Em banco novo os dois dizem a mesma coisa.
-- ----------------------------------------------------------------------------
alter table public.trades drop constraint if exists trades_type_check;
alter table public.trades add constraint trades_type_check
  check (type in ('take','stop','zero'));
```

- [ ] **Passo 5: Blindar o import em lote**

Em `js/services/trades.js`, logo abaixo de `const TABLE = 'trades';` (linha 10):

```js
// Espelha o check `trades_type_check` do banco: tipo fora desta lista derrubaria
// o insert inteiro do lote no Postgres
const TIPOS_VALIDOS = new Set(['take', 'stop', 'zero']);
```

Em `bulkImportTrades`, troque o corpo do `list.forEach` (linhas 137-153) por:

```js
    list.forEach((t, position) => {
      // Tipo desconhecido (arquivo antigo ou adulterado) cai em take, como sempre
      const tipo = TIPOS_VALIDOS.has(t.type) ? t.type : 'take';
      rows.push({
        user_id:     userId,
        block_index: Number(blockIdx) + blockOffset,
        position,
        asset:       String(t.asset || '').toUpperCase().slice(0, 64),
        type:        tipo,
        // 0x0 é sempre zero: JSON adulterado não cria empate de R$ 300
        pnl:         tipo === 'zero' ? 0 : (Number(t.pnl) || 0),
        trade_date:  t.date || new Date().toISOString().slice(0, 10),
        notes:       t.notes || null,
        // Backup JSON não carrega imagem (elas vivem no Storage)
        images:      [],
        // O R:R vai no backup, mas é normalizado na volta: arquivo antigo não
        // tem o campo, e um valor estragado no JSON não pode derrubar o lote
        risk_reward: normalizaRiskReward(t.riskReward)
      });
    });
```

- [ ] **Passo 6: Rodar e ver passar**

```bash
node "<SCRATCH>/rig/verificar-0x0.mjs"
```

Esperado: `Tudo passou.`

- [ ] **Passo 7: Aplicar o SQL no Supabase (manual, do Nikolas)**

Cole a **seção 10** no SQL Editor do Supabase e execute. Sem isso, gravar um 0x0 em
produção volta `new row for relation "trades" violates check constraint
"trades_type_check"`. O stub local não pega esse erro — a verificação acima passa de
qualquer jeito.

- [ ] **Passo 8: Commit**

```bash
cd "C:/Users/nikol/Desktop/Niko/projetos/apps/Monolith"
git add supabase/schema.sql js/services/trades.js
git commit -m "Abre o type da operação para o resultado 0x0"
```

---

### Tarefa 2: Modal registra o 0x0 com o valor travado

**Arquivos:**
- Modificar: `index.html:927-930` (opções do `#trade-type`)
- Modificar: `app.js:1120-1152` (`openTradeModal`), `app.js:1174-1202` (`handleSaveTrade`),
  `app.js:1424` (listeners do modal)
- Verificação: `<SCRATCH>/rig/verificar-0x0.mjs` (cenário 2)

**Interfaces:**
- Consome: o `type: 'zero'` já aceito pelo banco (Tarefa 1).
- Produz: `aplicarModoResultado(tipo)` — função de módulo em `app.js`, sem retorno,
  idempotente, que trava `#trade-pnl` em `0` quando `tipo === 'zero'` e devolve o valor
  guardado quando sai do 0x0. As Tarefas 3 e 4 não a chamam; só as verificações a exercem
  pela UI.

- [ ] **Passo 1: Escrever o cenário 2 (falhando)**

Insira em `verificar-0x0.mjs`, antes do bloco `// ---- Resultado`:

```js
// ---------------------------------------------------------------- Cenário 2
console.log('\n2. Modal: 0x0 trava o valor e devolve o que estava digitado');
{
  const page = await novaPagina();
  await page.click('[data-tab="trading-journal"]');
  await dormir(300);
  await page.click('#btn-new-trade-header');
  await page.waitForSelector('#trade-modal.active');

  const temOpcao = await page.$eval('#trade-type', (s) =>
    [...s.options].some((o) => o.value === 'zero'));
  conferir('existe a opção zero no seletor', temOpcao);

  await page.type('#trade-pnl', '42');
  await page.select('#trade-type', 'zero');
  await dormir(150);
  const travado = await page.$eval('#trade-pnl', (i) => ({ v: i.value, d: i.disabled }));
  conferir('trava em 0 e desabilita', travado.d === true && Number(travado.v) === 0,
    JSON.stringify(travado));

  await page.select('#trade-type', 'take');
  await dormir(150);
  const devolvido = await page.$eval('#trade-pnl', (i) => ({ v: i.value, d: i.disabled }));
  conferir('devolve o 42 ao sair do 0x0', devolvido.d === false && devolvido.v === '42',
    JSON.stringify(devolvido));

  // Ida e volta duas vezes não pode gravar "0" por cima do valor real
  await page.select('#trade-type', 'zero');
  await page.select('#trade-type', 'zero');
  await page.select('#trade-type', 'stop');
  await dormir(150);
  const aindaTem = await page.$eval('#trade-pnl', (i) => i.value);
  conferir('idempotente: o 42 sobrevive a duas travadas', aindaTem === '42', aindaTem);

  // Salvar de fato como 0x0
  await page.select('#trade-type', 'zero');
  await page.type('#trade-asset', 'WDO');
  await page.click('#btn-submit-modal');
  await dormir(1200);
  const db = await lerBanco(page);
  const salvo = db.trades.find((t) => t.asset === 'WDO');
  conferir('salva type zero com pnl 0',
    salvo && salvo.type === 'zero' && Number(salvo.pnl) === 0, JSON.stringify(salvo));

  // Reabrir para uma operação nova: o campo não pode vir travado da anterior
  await page.click('#btn-new-trade-header');
  await page.waitForSelector('#trade-modal.active');
  await dormir(200);
  const novo = await page.$eval('#trade-pnl', (i) => ({ v: i.value, d: i.disabled }));
  conferir('modal novo abre destravado e vazio', novo.d === false && novo.v === '',
    JSON.stringify(novo));
  await page.close();
}
```

- [ ] **Passo 2: Rodar e ver falhar**

```bash
node "<SCRATCH>/rig/verificar-0x0.mjs"
```

Esperado: FALHA já em `existe a opção zero no seletor`.

- [ ] **Passo 3: Acrescentar a opção no seletor**

Em `index.html`, depois da linha 929 (`<option value="stop">Stop (Prejuízo)</option>`):

```html
                    <option value="zero">0x0 (Zero a zero)</option>
```

- [ ] **Passo 4: Escrever `aplicarModoResultado`**

Em `app.js`, logo antes de `function openTradeModal` (linha 1120):

```js
/**
 * Ajusta o campo de valor ao tipo escolhido. No 0x0 o resultado é sempre 0,00 —
 * o campo trava para não existir empate com número. O que o usuário já tinha
 * digitado fica no dataset e volta quando ele sai do 0x0; sem isso, mexer no
 * seletor sem querer apagaria o valor.
 *
 * Idempotente de propósito: o valor só é guardado na PRIMEIRA travada
 * (`!disabled`), senão chamar duas vezes seguidas salvaria o "0" por cima do
 * número real e o usuário nunca mais o veria.
 */
function aplicarModoResultado(tipo) {
  if (tipo === 'zero') {
    if (!DOM.tradePnL.disabled) DOM.tradePnL.dataset.valorAntesDoZero = DOM.tradePnL.value;
    DOM.tradePnL.value = '0';
    DOM.tradePnL.disabled = true;
  } else if (DOM.tradePnL.disabled) {
    DOM.tradePnL.value = DOM.tradePnL.dataset.valorAntesDoZero ?? '';
    DOM.tradePnL.disabled = false;
  }
}
```

- [ ] **Passo 5: Aplicar o modo ao abrir o modal**

Em `app.js`, em `openTradeModal`, entre o fim do `else` (linha 1149, `}`) e
`DOM.tradeModal.classList.add('active')` (linha 1150), insira:

```js
  // `form.reset()` não desfaz `disabled` nem limpa dataset: sem este
  // destravamento, fechar o modal num 0x0 deixaria o campo travado na próxima
  // operação, e o valor guardado vazaria de uma operação para a outra.
  DOM.tradePnL.disabled = false;
  delete DOM.tradePnL.dataset.valorAntesDoZero;
  aplicarModoResultado(DOM.tradeType.value);
```

- [ ] **Passo 6: Reagir à troca de tipo**

Em `app.js`, em `setupEventListeners`, logo abaixo da linha 1424
(`DOM.tradeForm.addEventListener('submit', ...)`):

```js
  DOM.tradeType.addEventListener('change', () => aplicarModoResultado(DOM.tradeType.value));
```

- [ ] **Passo 7: Salvar com P&L zero**

Em `app.js`, em `handleSaveTrade`, troque a validação da linha 1190:

```js
  if (!asset || isNaN(rawPnl) || !date) {
```

por:

```js
  // No 0x0 o valor não é digitado — vem travado em 0 —, então não é obrigatório
  if (!asset || !date || (type !== 'zero' && isNaN(rawPnl))) {
```

E troque a linha 1202:

```js
  const pnl = type === 'stop' ? -Math.abs(rawPnl) : Math.abs(rawPnl);
```

por:

```js
  // 0x0 é sempre 0; nos outros, o tipo é que dá o sinal do valor absoluto digitado
  const pnl = type === 'zero' ? 0
            : type === 'stop' ? -Math.abs(rawPnl)
            : Math.abs(rawPnl);
```

- [ ] **Passo 8: Rodar e ver passar**

```bash
node "<SCRATCH>/rig/verificar-0x0.mjs"
```

Esperado: `Tudo passou.` (cenários 1 e 2).

- [ ] **Passo 9: Commit**

```bash
cd "C:/Users/nikol/Desktop/Niko/projetos/apps/Monolith"
git add index.html app.js
git commit -m "Registra a operação 0x0 pelo modal, com o valor travado em zero"
```

---

### Tarefa 3: Win rate ignora o 0x0, e o saldo zero deixa de ser "positivo"

**Arquivos:**
- Modificar: `app.js:485-492` (`computeStats`), `app.js:494-553` (`updateKPIs`),
  `app.js:820-880` (`renderDashboard`)
- Verificação: `<SCRATCH>/rig/verificar-0x0.mjs` (cenário 3)

**Interfaces:**
- Consome: `trade.type === 'zero'` gravado pelas Tarefas 1 e 2.
- Produz: `computeStats(trades)` passa a devolver
  `{ count, accumulated, winTrades, zeroTrades, decided, winRate, average }`, onde
  `decided = count - zeroTrades` e **`winRate` é `number | null`** — `null` quando não há
  nenhuma operação decidida. Todo consumidor precisa tratar o `null`; hoje são
  `updateKPIs` e `renderDashboard`.

- [ ] **Passo 1: Escrever o cenário 3 (falhando)**

Insira em `verificar-0x0.mjs`, antes do bloco `// ---- Resultado`:

```js
// ---------------------------------------------------------------- Cenário 3
console.log('\n3. KPIs: 0x0 fora do win rate, dentro do resto');
{
  const page = await novaPagina();
  await semear(page, [
    { type: 'take', pnl: 100 }, { type: 'take', pnl: 100 }, { type: 'take', pnl: 100 },
    { type: 'stop', pnl: -50 }, { type: 'stop', pnl: -50 },
    { type: 'zero', pnl: 0 },   { type: 'zero', pnl: 0 }
  ]);
  await page.click('[data-tab="trading-journal"]');
  await dormir(400);

  const txt = (sel) => page.$eval(sel, (e) => e.textContent.trim());
  conferir('win rate 60% (3 de 5, os dois 0x0 fora)', (await txt('#val-winrate')) === '60%',
    await txt('#val-winrate'));
  conferir('indicador diz 3 de 5', (await txt('#ind-winrate')).includes('3 de 5'),
    await txt('#ind-winrate'));
  conferir('registradas contam os 0x0 (7)', (await txt('#val-registered')) === '7',
    await txt('#val-registered'));

  // Bloco só de 0x0: não há taxa de acerto, e o saldo não é "positivo"
  await semear(page, [{ type: 'zero', pnl: 0 }, { type: 'zero', pnl: 0 }]);
  await page.click('[data-tab="trading-journal"]');
  await dormir(400);
  conferir('sem decididas, win rate vira —', (await txt('#val-winrate')) === '—',
    await txt('#val-winrate'));
  conferir('saldo zero é neutro', (await txt('#ind-accumulated')) === 'Saldo neutro',
    await txt('#ind-accumulated'));
  conferir('média zero é neutra', (await txt('#ind-average')) === 'Média neutra',
    await txt('#ind-average'));
  conferir('card do acumulado sem cor de alta',
    !(await page.$eval('#kpi-accumulated', (e) => e.classList.contains('win-trend'))));
  await page.close();
}
```

Os IDs acima foram conferidos no `index.html` — note que o card do acumulado é
`#kpi-accumulated`, **sem** o sufixo `-card` que o nome da variável (`kpiAccumulatedCard`)
sugere.

- [ ] **Passo 2: Rodar e ver falhar**

```bash
node "<SCRATCH>/rig/verificar-0x0.mjs"
```

Esperado: FALHA em `win rate 60%` (hoje sai 43%, 3 de 7) e em `saldo zero é neutro`.

- [ ] **Passo 3: Ensinar `computeStats` a separar o empate**

Em `app.js`, substitua `computeStats` (linhas 484-492) por:

```js
/** Estatísticas de uma lista de trades (bloco ativo ou conta inteira). */
function computeStats(trades) {
  const count = trades.length;
  const accumulated = trades.reduce((s, t) => s + t.pnl, 0);
  const winTrades = trades.filter(t => t.pnl > 0).length;
  // O 0x0 não é acerto nem erro: sai do denominador do win rate, mas segue
  // contando nas operações registradas e na média por operação — ele ocupou
  // um slot do bloco e foi uma operação executada.
  const zeroTrades = trades.filter(t => t.type === 'zero').length;
  const decided = count - zeroTrades;
  // null, não 0: bloco sem operação decidida não tem taxa de acerto para mostrar
  const winRate = decided > 0 ? Math.round((winTrades / decided) * 100) : null;
  const average = count > 0 ? (accumulated / count) : 0;
  return { count, accumulated, winTrades, zeroTrades, decided, winRate, average };
}
```

- [ ] **Passo 4: Atualizar os KPIs do diário**

Em `app.js`, na linha 495, troque a desestruturação por:

```js
  const { count, accumulated, winTrades, decided, winRate, average } = computeStats(trades);
```

Troque o bloco do acumulado (linhas 507-521) por:

```js
  if (count === 0) {
    DOM.valAccumulated.textContent = formatCurrency(accumulated);
    DOM.valAccumulated.className = 'kpi-value';
    DOM.indAccumulated.textContent = 'Sem operações';
  } else if (accumulated === 0) {
    // Bloco fechado exatamente no zero — com 0x0 no jogo isso acontece de
    // verdade, e chamar de "positivo" (verde) seria mentira
    DOM.valAccumulated.textContent = formatCurrency(accumulated);
    DOM.valAccumulated.className = 'kpi-value';
    DOM.indAccumulated.textContent = 'Saldo neutro';
  } else if (accumulated > 0) {
    DOM.valAccumulated.textContent = formatCurrencySigned(accumulated);
    DOM.valAccumulated.className = 'kpi-value pnl-positive';
    DOM.indAccumulated.textContent = 'Saldo positivo';
    DOM.kpiAccumulatedCard.classList.add('win-trend');
  } else {
    DOM.valAccumulated.textContent = formatCurrencySigned(accumulated);
    DOM.valAccumulated.className = 'kpi-value pnl-negative';
    DOM.indAccumulated.textContent = 'Saldo negativo';
    DOM.kpiAccumulatedCard.classList.add('loss-trend');
  }
```

Troque o bloco do win rate (linhas 523-529) por:

```js
  DOM.valWinrate.textContent = winRate === null ? '—' : `${winRate}%`;
  if (decided > 0) {
    DOM.indWinrate.textContent = `${winTrades} de ${decided} vitoriosos`;
    if (winRate >= 50) DOM.kpiWinrateCard.classList.add('win-trend');
  } else {
    DOM.indWinrate.textContent = 'Taxa de acerto do bloco';
  }
```

Troque o bloco da média (linhas 532-546) por:

```js
  if (count === 0) {
    DOM.valAverage.textContent = formatCurrency(average);
    DOM.valAverage.className = 'kpi-value';
    DOM.indAverage.textContent = 'Média de lucro/prejuízo';
  } else if (average === 0) {
    DOM.valAverage.textContent = formatCurrency(average);
    DOM.valAverage.className = 'kpi-value';
    DOM.indAverage.textContent = 'Média neutra';
  } else if (average > 0) {
    DOM.valAverage.textContent = formatCurrencySigned(average);
    DOM.valAverage.className = 'kpi-value pnl-positive';
    DOM.indAverage.textContent = 'Média positiva';
    DOM.kpiAverageCard.classList.add('win-trend');
  } else {
    DOM.valAverage.textContent = formatCurrencySigned(average);
    DOM.valAverage.className = 'kpi-value pnl-negative';
    DOM.indAverage.textContent = 'Média negativa';
    DOM.kpiAverageCard.classList.add('loss-trend');
  }
```

E o resumo do rodapé (linhas 548-552) por:

```js
  DOM.summaryTradesCount.textContent = count;
  DOM.summaryWinrate.textContent = winRate === null ? '—' : `${winRate}%`;
  DOM.summaryPL.textContent = formatCurrency(accumulated);
  DOM.summaryPL.className = accumulated > 0 ? 'pnl-positive'
                          : accumulated < 0 ? 'pnl-negative'
                          : '';
```

(a linha `if (count === 0) DOM.summaryPL.className = '';` sai — o caso zero já cai no
ramo neutro.)

- [ ] **Passo 5: Aplicar o mesmo ao dashboard**

Em `app.js`, `renderDashboard`, repita exatamente as quatro trocas do Passo 4 nos
elementos `DOM.dash*`, com duas diferenças de texto:

- o texto padrão do indicador de win rate é `'Taxa de acerto da conta'` (linha 856), não
  `'Taxa de acerto do bloco'`;
- a linha 880 vira:

```js
  DOM.dashSummaryPL.className = accumulated > 0 ? 'pnl-positive'
                              : accumulated < 0 ? 'pnl-negative'
                              : '';
```

A desestruturação da linha 823 também passa a incluir `decided`.

- [ ] **Passo 6: Rodar e ver passar**

```bash
node "<SCRATCH>/rig/verificar-0x0.mjs"
```

Esperado: `Tudo passou.` (cenários 1 a 3).

- [ ] **Passo 7: Commit**

```bash
cd "C:/Users/nikol/Desktop/Niko/projetos/apps/Monolith"
git add app.js
git commit -m "Tira o 0x0 do denominador do win rate e reconhece saldo neutro"
```

---

### Tarefa 4: Card e tabela mostram o 0x0 como neutro

**Arquivos:**
- Modificar: `app.js:555-599` (`renderGridView`), `app.js:676-718` (`renderListView`)
- Modificar: `style.css:735-740` (`.pnl-*`), `style.css:1181` (`.table-type`),
  `style.css:1104-1114` (fim das variações de slot)
- Verificação: `<SCRATCH>/rig/verificar-0x0.mjs` (cenário 4)

**Interfaces:**
- Consome: `trade.type === 'zero'` e o `winRate` nulo já tratados.
- Produz: as classes CSS `slot-zero` (card do grid), `type-zero` (célula de tipo da
  tabela) e `pnl-neutro` (célula de valor da tabela). Nenhuma outra tarefa depende delas.

- [ ] **Passo 1: Escrever o cenário 4 (falhando)**

Insira em `verificar-0x0.mjs`, antes do bloco `// ---- Resultado`:

```js
// ---------------------------------------------------------------- Cenário 4
console.log('\n4. Card e tabela do 0x0 em neutro');
{
  const page = await novaPagina();
  await semear(page, [{ type: 'take', pnl: 100 }, { type: 'zero', pnl: 0, asset: 'WDO' }]);
  await page.click('[data-tab="trading-journal"]');
  await dormir(400);

  const cards = await page.$$eval('.grid-slot.slot-filled', (els) =>
    els.map((e) => ({ classes: e.className, texto: e.textContent })));
  const cardZero = cards.find((c) => c.texto.includes('WDO'));
  conferir('card do 0x0 usa slot-zero', !!cardZero && cardZero.classes.includes('slot-zero'),
    cardZero && cardZero.classes);
  conferir('card do 0x0 não é verde nem vermelho',
    !!cardZero && !cardZero.classes.includes('slot-win') && !cardZero.classes.includes('slot-loss'),
    cardZero && cardZero.classes);
  conferir('card do 0x0 sem sinal de +', !!cardZero && !cardZero.texto.includes('+'),
    cardZero && cardZero.texto);
  conferir('card do take continua verde',
    cards.some((c) => c.classes.includes('slot-win')));

  // A tabela é a outra visão do mesmo bloco
  await page.click('#btn-toggle-list');
  await dormir(400);
  const linhas = await page.$$eval('#table-body-container tr', (els) =>
    els.map((tr) => ({
      tipo: tr.querySelector('.table-type')?.textContent.trim(),
      tipoClasses: tr.querySelector('.table-type')?.className,
      pnl: tr.querySelector('.table-pnl')?.textContent.trim()
    })));
  const linhaZero = linhas.find((l) => l.tipo === '0x0');
  conferir('tabela rotula 0x0', !!linhaZero, JSON.stringify(linhas));
  conferir('rótulo 0x0 em classe neutra',
    !!linhaZero && linhaZero.tipoClasses.includes('type-zero')
      && !linhaZero.tipoClasses.includes('pnl-positive'), linhaZero && linhaZero.tipoClasses);
  conferir('valor do 0x0 sem +', !!linhaZero && !linhaZero.pnl.includes('+'),
    linhaZero && linhaZero.pnl);
  await page.close();
}
```

- [ ] **Passo 2: Rodar e ver falhar**

```bash
node "<SCRATCH>/rig/verificar-0x0.mjs"
```

Esperado: FALHA em `card do 0x0 usa slot-zero` (hoje ele cai em `slot-win`, porque
`pnl >= 0`) e em `tabela rotula 0x0` (hoje sai "Stop", porque o rótulo é um ternário de
duas vias).

- [ ] **Passo 3: Card do grid**

Em `app.js`, `renderGridView`, troque as linhas 562-563:

```js
      slotEl.className = `grid-slot slot-filled ${trade.pnl >= 0 ? 'slot-win' : 'slot-loss'}`
                       + (temImagem ? ' slot-has-image' : '');
```

por:

```js
      // O 0x0 vem antes do teste de sinal: pnl 0 passaria por "win"
      const classeResultado = trade.type === 'zero' ? 'slot-zero'
                            : trade.pnl >= 0        ? 'slot-win'
                            : 'slot-loss';
      slotEl.className = `grid-slot slot-filled ${classeResultado}`
                       + (temImagem ? ' slot-has-image' : '');
```

E a linha 575:

```js
          <div class="slot-pnl">${trade.pnl >= 0 ? '+' : ''} ${formatCurrency(trade.pnl)}</div>
```

por:

```js
          <div class="slot-pnl">${trade.type === 'zero' ? '' : trade.pnl >= 0 ? '+' : ''} ${formatCurrency(trade.pnl)}</div>
```

- [ ] **Passo 4: Tabela**

Em `app.js`, `renderListView`, troque as linhas 689-691:

```js
    const pnlClass = trade.pnl >= 0 ? 'pnl-positive' : 'pnl-negative';
    const typeLabel = trade.type === 'take' ? 'Take' : 'Stop';
    const typeClass = trade.type === 'take' ? 'pnl-positive' : 'pnl-negative';
```

por:

```js
    const ehZero    = trade.type === 'zero';
    const pnlClass  = ehZero ? 'pnl-neutro' : trade.pnl >= 0 ? 'pnl-positive' : 'pnl-negative';
    const typeLabel = ehZero ? '0x0' : trade.type === 'take' ? 'Take' : 'Stop';
    const typeClass = ehZero ? 'type-zero' : trade.type === 'take' ? 'pnl-positive' : 'pnl-negative';
```

E a célula do valor (linha 697):

```js
      <td class="table-pnl ${pnlClass}">${trade.pnl >= 0 ? '+' : ''}${formatCurrency(trade.pnl)}</td>
```

por:

```js
      <td class="table-pnl ${pnlClass}">${ehZero ? '' : trade.pnl >= 0 ? '+' : ''}${formatCurrency(trade.pnl)}</td>
```

- [ ] **Passo 5: CSS do neutro**

Em `style.css`, depois de `.pnl-negative` (linha 740):

```css
/* 0x0: nem lucro nem prejuízo. Sai de --text-secondary, que já existe nos dois
   temas — o neutro acompanha dark e light sem cor nova no arquivo. */
.pnl-neutro {
  color: var(--text-secondary) !important;
}
```

Depois de `.grid-slot.slot-loss .slot-pnl` (linha 1114):

```css
.grid-slot.slot-zero {
  background-color: var(--bg-card-hover);
  border: 1px solid var(--border-color);
}
.grid-slot.slot-zero:hover {
  border-color: var(--border-hover);
}
.grid-slot.slot-zero .slot-pnl {
  color: var(--text-secondary);
}
```

E logo depois do bloco `.table-type` (que termina na linha 1185):

```css
.table-type.type-zero {
  color: var(--text-secondary);
}
```

- [ ] **Passo 6: Rodar e ver passar**

```bash
node "<SCRATCH>/rig/verificar-0x0.mjs"
```

Esperado: `Tudo passou.` (cenários 1 a 4).

- [ ] **Passo 7: Commit**

```bash
cd "C:/Users/nikol/Desktop/Niko/projetos/apps/Monolith"
git add app.js style.css
git commit -m "Pinta o 0x0 de neutro no card e na tabela"
```

---

### Tarefa 5: Regressão, tema light e documentação

**Arquivos:**
- Modificar: `CLAUDE.md` (seções "Modelo de dados" e "Conceitos do domínio")
- Verificação: `<SCRATCH>/rig/verificar-0x0.mjs` (cenário 5)

**Interfaces:**
- Consome: tudo das Tarefas 1 a 4. Não produz nada consumido por outra tarefa.

- [ ] **Passo 1: Escrever o cenário 5 — regressão de take/stop e tema light**

Insira em `verificar-0x0.mjs`, antes do bloco `// ---- Resultado`:

```js
// ---------------------------------------------------------------- Cenário 5
console.log('\n5. Regressão: take e stop intactos, e o 0x0 no tema light');
{
  const page = await novaPagina();
  await semear(page, [
    { type: 'take', pnl: 200 }, { type: 'stop', pnl: -100 }, { type: 'zero', pnl: 0 }
  ]);
  await page.click('[data-tab="trading-journal"]');
  await dormir(400);

  const txt = (sel) => page.$eval(sel, (e) => e.textContent.trim());
  conferir('win rate 50% (1 de 2 decididas)', (await txt('#val-winrate')) === '50%',
    await txt('#val-winrate'));
  conferir('acumulado +100 segue positivo',
    (await txt('#ind-accumulated')) === 'Saldo positivo', await txt('#ind-accumulated'));

  // Editar o 0x0 para stop: o campo tem que reabilitar e gravar negativo
  const cards = await page.$$('.grid-slot.slot-filled');
  await cards[2].click();
  await page.waitForSelector('#trade-modal.active');
  await dormir(200);
  const aoEditar = await page.$eval('#trade-pnl', (i) => i.disabled);
  conferir('0x0 existente reabre com o campo travado', aoEditar === true, String(aoEditar));
  await page.select('#trade-type', 'stop');
  await dormir(150);
  await page.$eval('#trade-pnl', (i) => { i.value = ''; });
  await page.type('#trade-pnl', '50');
  await page.click('#btn-submit-modal');
  await dormir(1200);
  const db = await lerBanco(page);
  const editado = db.trades.find((t) => t.position === 2);
  conferir('editar 0x0 para stop grava -50',
    editado && editado.type === 'stop' && Number(editado.pnl) === -50, JSON.stringify(editado));

  // Tema light: o neutro do 0x0 tem que continuar legível
  await semear(page, [{ type: 'zero', pnl: 0, asset: 'WDO' }]);
  await page.click('#btn-footer-menu-toggle');
  await dormir(200);
  await page.click('#btn-theme-toggle');
  await dormir(500);
  await page.click('[data-tab="trading-journal"]');
  await dormir(400);
  const contraste = await page.$eval('.grid-slot.slot-zero .slot-pnl', (e) => {
    const s = getComputedStyle(e);
    return { cor: s.color, fundo: getComputedStyle(e.closest('.grid-slot')).backgroundColor };
  });
  conferir('no light o texto do 0x0 não some no fundo',
    contraste.cor !== contraste.fundo, JSON.stringify(contraste));
  await page.close();
}
```

- [ ] **Passo 2: Rodar a bateria inteira**

```bash
node "<SCRATCH>/rig/verificar-0x0.mjs"
```

Esperado: `Tudo passou.` nos cinco cenários. Se algo falhar aqui, é regressão das tarefas
anteriores — corrija antes de seguir.

- [ ] **Passo 3: Atualizar o `CLAUDE.md` do Monolith**

Na seção "Modelo de dados", troque em `type` (`'take'` | `'stop'`) por:

```md
  `type` (`'take'` | `'stop'` | `'zero'`),
```

Na seção "Conceitos do domínio", troque o item de take/stop por:

```md
- **`take` / `stop` / `zero`:** `take` = lucro (pnl positivo), `stop` = prejuízo (pnl
  gravado negativo), `zero` = **0x0**, operação encerrada no preço de entrada, com pnl
  sempre 0. No modal o usuário digita o **valor absoluto**; o sinal vem do tipo. No 0x0 o
  campo de valor **trava em 0,00**, e o que estava digitado volta se ele trocar o tipo de
  novo (guardado em `dataset.valorAntesDoZero`) — `form.reset()` não desfaz `disabled`,
  então `openTradeModal` destrava sempre antes de reaplicar o modo.
```

E o item de KPIs por:

```md
- **KPIs:** operações registradas, resultado acumulado, win rate, média por operação. O
  gráfico é a **soma acumulada** do P&L do bloco. O **0x0 fica fora do denominador do win
  rate** (`takes ÷ (total − zeros)`) — empate não é acerto nem erro —, mas continua
  contando nas operações registradas e na média por operação, porque ocupou um slot do
  bloco. Bloco sem nenhuma operação decidida mostra `—`, não `0%`, e saldo/média
  exatamente zero são rotulados "neutro", sem cor.
```

- [ ] **Passo 4: Limpar o ambiente**

```bash
kill %1   # ou pare o `python -m http.server 8077`
```

O rig fica no scratchpad (fora do repo) e não precisa ser apagado — confirme só que
`git status` não mostra `node_modules` nem `.json` de teste dentro do repositório.

- [ ] **Passo 5: Commit**

```bash
cd "C:/Users/nikol/Desktop/Niko/projetos/apps/Monolith"
git add CLAUDE.md
git commit -m "Documenta o resultado 0x0 nas convenções do Monolith"
```

- [ ] **Passo 6: Lembrete final ao Nikolas**

Confirmar que a **seção 10 do `schema.sql` foi aplicada no Supabase** (Tarefa 1, Passo 7).
Sem ela, tudo funciona local e produção recusa o primeiro 0x0 com
`violates check constraint "trades_type_check"`.
